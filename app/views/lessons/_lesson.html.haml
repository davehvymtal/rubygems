.card
  .card-header
  /se agrega objeto @courses al path
    = link_to lesson.title, course_lesson_path(@course, lesson)
  /se agrega validacion de politicas si el usuario puede ver los datos de la leccion show
  - if policy(lesson).show?
    .card-body
      = lesson.content
      = lesson.course
  /se agrega validacion de politicas si el usuario puede ver los links de edicion, y destroy
  - if policy(lesson).edit?
    .card-footer
      /se modifica direccionamiento al nuevo path COURSES/COURSE_FRIENDLYID/UPDATE
      = link_to 'Edit', edit_course_lesson_path(@course,lesson), class: 'btn btn-sm btn-warning' 
      /se envia parametro @courses
      = link_to 'Destroy', [@course,lesson], method: :delete, data: { confirm: 'Are you sure?' }, class: 'btn btn-sm btn-danger' 
%p
